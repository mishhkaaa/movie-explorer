# 🎬 Movie Explorer

A full-stack MERN app to search movies, add favorites, and manage your personal list with login, dark mode, and session persistence.

## 🚀 Features
- User authentication (JWT)
- Search & save favorite movies
- Dark/Light mode toggle
- Loaders and error alerts
- Persistent user session

## 🛠️ Stack
**Frontend:** React, Context API, Axios, CSS  
**Backend:** Node.js, Express, MongoDB, JWT

## ⚙️ Setup
```bash
cd backend && npm install && npm start
cd ../frontend && npm install && npm start

Backend → http://localhost:5000
Frontend → http://localhost:3000

